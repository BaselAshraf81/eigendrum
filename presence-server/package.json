{
  "name": "eigendrum-presence",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone WebSocket presence server for eigendrum.com. Counts concurrently open connections and broadcasts the count. Runs on its own VPS, entirely separate from the static site's Vercel deployment.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "8.18.0"
  }
}
